#pragma once
// SHIM — not upstream. See ../VENDORED.md.
//
// FreeCAD's generated build configuration. planegcs includes it but uses nothing from it; the
// feature macros it defines are for other parts of FreeCAD.
